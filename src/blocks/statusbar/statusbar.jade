mixin statusbar(onlyColor)
  .statusbar
    span.statusbar__change-theme
    if (!onlyColor && user)
      span(style='float: right;') Spaces: 2, JavaScript
      span#statusbar__position Line: ~, Column: ~
